# Backend Environment Variables Example
# Copy this file to .env and update values as needed

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
# For local MongoDB:
MONGODB_URI=mongodb://localhost:27017/standard-engineering

# For MongoDB Atlas (Cloud):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/standard-engineering

# JWT Configuration
JWT_SECRET=change_this_secret_key_in_production_use_strong_random_string
JWT_EXPIRE=7d

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Email Configuration (Optional - for future use)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_password

# File Upload (Optional - for future use)
# MAX_FILE_SIZE=5242880  # 5MB in bytes
# UPLOAD_DIR=./uploads
